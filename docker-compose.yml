version: '3.8'

services:
  bilibili-mcp:
    build: .
    container_name: bilibili-mcp
    ports:
      - "8080:8080"
    environment:
      - BILI_COOKIE=${BILI_COOKIE}
      - MCP_PORT=8080
    restart: always
